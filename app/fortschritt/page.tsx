import { ProgressDashboard } from "@/components/ProgressDashboard";

export const metadata = { title: "Lernfortschritt — ARV 2" };

export default function FortschrittPage() {
  return <ProgressDashboard />;
}
