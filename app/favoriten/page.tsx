import { FavouritesSession } from "@/components/FilteredPractice";

export const metadata = { title: "Gemerkte Fragen — ARV 2" };

export default function FavoritenPage() {
  return <FavouritesSession />;
}
